export const formatter = new Intl.NumberFormat('en', { notation: 'compact' });
export const sortDescending = (a: number, b: number) => b - a;
export const sortAscending = (a: number, b: number) => a - b;
